{
  "name": "seu-politico",
  "version": "1.0.0",
  "private": true,
  "description": "Ferramenta de transparência cidadã (frontend + API proxy).",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node backend/server.js",
    "precompute": "node scripts/precompute.js",
    "prebuild": "node scripts/precompute.js --soft",
    "test": "cd backend && node --test"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.2",
    "adm-zip": "^0.5.18",
    "cors": "^2.8.5",
    "csv-parse": "^7.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
